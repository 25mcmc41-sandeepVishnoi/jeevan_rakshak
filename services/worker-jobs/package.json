{
  "name": "@jr/worker-jobs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/worker.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"Wire eslint in next iteration\"",
    "test": "echo \"Wire tests in next iteration\""
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
